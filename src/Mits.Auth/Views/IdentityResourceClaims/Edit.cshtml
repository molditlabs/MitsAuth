
@model IdentityResourceClaimEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="@Url.Action("Index","Home")">Home</a></li>
        <li class="breadcrumb-item"><a href="@Url.Action("Index","IdentityResources")">Identity Resources</a></li>
        <li class="breadcrumb-item"><a href="@Url.Action("Details","IdentityResources", new { id=Model.IdentityResourceId})">Details</a></li>
        <li class="breadcrumb-item active" aria-current="page">Edit Claim</li>
    </ol>
</nav>

<div class="row">
    <div class="col-lg-12">

        <div class="card">
            <div class="card-header">
                Edit Identity Resource Claim
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-lg-12">

                        <partial name="_ValidationSummary" />

                    </div>
                </div>

                <form asp-action="Edit">

                    <input hidden asp-for="@Model.Id" />
                    <input hidden asp-for="@Model.IdentityResourceId" />

                    <div class="form-group">
                        <label>Type</label>
                        <input asp-for="@Model.Type" class="form-control" />
                    </div>


                    <div class="form-group">
                        <a asp-action="Details" asp-controller="IdentityResources" asp-route-id="@Model.IdentityResourceId" class="btn btn-info">Cancel</a>
                        <input type="submit" class="btn btn-primary" value="Save" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
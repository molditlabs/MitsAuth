
@model ClientGrantTypeEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="@Url.Action("Index","Home")">Home</a></li>
        <li class="breadcrumb-item"><a href="@Url.Action("Index","Clients")">Clients</a></li>
        <li class="breadcrumb-item"><a href="@Url.Action("Details","Clients", new { id=Model.ClientId})">Details</a></li>
        <li class="breadcrumb-item active" aria-current="page">Edit Grant Type</li>
    </ol>
</nav>

<div class="row">
    <div class="col-lg-12">

        <div class="card">
            <div class="card-header">
                Edit Client Grant Type
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-lg-12">

                        <partial name="_ValidationSummary" />

                    </div>
                </div>

                <form asp-action="Edit">

                    <input hidden asp-for="@Model.Id" />
                    <input hidden asp-for="@Model.ClientId" />

                    <div class="form-group">
                        <label>Grant Type</label>
                        <input asp-for="@Model.GrantType" class="form-control" />
                    </div>

                    <div class="form-group">
                        <a asp-action="Details" asp-controller="Clients" asp-route-id="@Model.ClientId" class="btn btn-info">Cancel</a>
                        <input type="submit" class="btn btn-primary" value="Save" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>